#Git

## Stash
git stash push -m “name” // instead of just git stash, stash with a name
git stash list

git stash pop // the most recent
git stash apply stash^{/<regex>} // git stash apply stash^{/name}
git stash apply 0 = git stash apply stash@{0}

git stash drop // the most recent
git stash drop 0  = git stash drop stash@{0}

## Set the username/email for a specific repository

git config user.name "Your project specific name"
git config user.email "your@project-specific-email.com"

git config --get user.name // verify
git config --get user.email
